name: vidflux
description: A new Flutter package project.
version: 0.2.0
author:
homepage:

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity: ^0.4.8+2  
  provider: ^4.0.4
  rxdart: ^0.23.1
  screen: ^0.0.5
  video_player: ^0.10.8+1  
  volume_watcher: ^1.1.0
  equatable: ^1.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
